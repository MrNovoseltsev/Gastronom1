/node_modules
/.next
/out
/build
*.tsbuildinfo
next-env.d.ts
.DS_Store
._*
.env*.local

# Собранный сайт лежит в корне ветки (index.html, 404.html, _next/, .nojekyll)
# и КОММИТИТСЯ — GitHub Pages раздаёт его прямо из корня ветки exmpl2.
